node_modules/
package-lock.json
/pkg
/*.tgz
/dist
DEBUG-*
# Ignore Yarn's own files, but not the patches: they are source, and without them
# the dependency installs unfixed. The contents must be excluded rather than the
# directory itself, or git never looks inside to find the exception.
/.yarn/*
!/.yarn/patches
/.vscode
